function M = fuseMaps(maps)
    % it takes an array of maps and 
    % fuse them into a single map
    [dimx, dimy] = size(maps);
    numberOfMaps = max(dimx, dimy);
    for i=1:numberOfMaps
    end
end

